import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // webpack: (config) => {
  //   config.resolve.alias["threepipe"] = path.resolve(__dirname, "node_modules/threepipe/dist/index.mjs");
  //   return config;
  // },
};

export default nextConfig;
